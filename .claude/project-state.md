# Project State

## Current Goal

Свадебный сайт-конструктор: лендинг → лид → админка (согласование/оплата) → публичный сайт на поддомене. Шаги 10 и 11 закрыты (5 шаблонов + живые демо-сайты) — следующий этап ещё не определён пользователем.

Пройденные шаги (по коммитам `git log --oneline | grep -i "step"`, это единственный источник нумерации — отдельного файла с планом в репозитории нет; фразы "см. план"/"see plan" в комментариях кода отсылают к обсуждению до этого репозитория, не к файлу):

1. Фундамент: Next.js + Prisma + Docker + сессионная auth админа.
2. Дизайн-система: примитивы + Zod-схемы контента блоков.
3. Первый полный шаблон (Tuscany) end-to-end.
4. Публичный сайт по поддомену (`proxy.ts` + lookup проекта).
5. Лендинг: лид-форма, публичный конфигуратор с live-preview и редактированием контента.
6. Админка: список лидов, конверсия лида в проект, редактор проекта.
7. Реальный RSVP-эндпоинт, гейтится статусом оплаты.
8. Личный кабинет клиента по токену (`app/client/[token]`).
9. Telegram-уведомления о новых RSVP.
10. 5 визуальных шаблонов (Tuscany, Old Money, Editorial Ч-Б, Pink Sketch, Moody Paper) — закрыт.
11. **(текущий, закрыт)** Реальные демо-сайты с реальным контентом для каждого шаблона — см. Current Milestone ниже.

## Current Milestone

Шаг 11 "живые демо-сайты" — **закрыт**. Для каждого из 5 шаблонов есть реальный опубликованный `Project` на своём поддомене (`demo-<template>.<APP_BASE_DOMAIN>`, сидится через `npm run seed:demos` → `prisma/seed-demos.ts`), с общим для всех набором контента/фото (пара «Александр и Мария» — как в фолбэке конфигуратора) и своей палитрой дресс-кода. Свотчи в `components/landing/examples.tsx` ведут на эти живые сайты и показывают реальный скриншот каждого (не общее стоковое фото — оно одинаковое у всех 5 и не различало бы стили). Отревьюено через `/feature-review` + точечный код-ревью после правок, закоммичено (`9780718`, плюс более ранний точечный фикс `997f4b9`).

## Completed

- 5 шаблонов реализованы и зарегистрированы в `lib/templates/registry.ts`: `tuscany`, `old-money`, `editorial-bw`, `pink-sketch`, `moody-paper` — у каждого свой рефборд и "фишка" (см. CLAUDE.md §9–10).
- Illustrated Storybook (несостоявшаяся 5-я попытка) — реализован, отклонён пользователем как не подходящий эстетически, полностью удалён (`c05dfca`).
- Архитектурный баг чередования фона блоков исправлен: было хардкожено по типу блока, стало — динамически по позиции среди включённых блоков (`alternatingBlocks`/`alternateDark`, `00b8aba`). Применено к Tuscany и Old Money.
- Pink Sketch: рамка фото доведена итеративно (wobbly SVG → прямая рамка → чёткая тень → тонкая рамка + блюр-тень), поправлен клиппинг `SquigglyDivider`.
- Old Money: обложка и RSVP переработаны под рефборд, доработан таймер (переиспользует `coverPhotoUrl`).
- Moody Paper: реализован (переписан с нуля после отклонённой v1), отревьюен и закоммичен вместе с `CLAUDE.md` и этим файлом (`6a6fd84`).
- Критический баг: вставка Windows-пути (`C:\...`) в поле URL фото роняла `next/image` — `isRenderableUrl` (`components/primitives/photo-grid.tsx`) принимал любую строку, которую `new URL()` не бросает, включая опаковые схемы вида `c:`. Исправлено: теперь требует `protocol === "http:" | "https:"` (`997f4b9`).
- Шаг 11 (демо-сайты): `prisma/seed-demos.ts` создаёт/обновляет 5 `Project` (по одному на шаблон, `status: PUBLISHED`, без `Theme` — используют дефолтную палитру шаблона). Общий контент/фото согласованы с пользователем (стоковые фото Pexels, европейская внешность и стройные типажи — часть исходного набора была отклонена и заменена). `lib/site-url.ts` — общая функция построения публичного URL проекта, вынесена из `app/admin/projects/[id]/page.tsx`, переиспользуется в `examples.tsx`. Превью на лендинге — собственные скриншоты живых демо-сайтов (`public/images/demo-previews/`, не сток) — закоммичено `9780718`.

## In Progress

Ничего — шаг 11 закрыт, следующий этап не определён.

## Next Steps

1. Следующий этап не определён — дождаться указания пользователя.
2. Догнать пропущенный визуальный QA по Moody Paper (см. Known Issues) — было отложено пользователем, всё ещё не сделано.

## Architecture Decisions

- **Динамическое чередование фона** (`TemplateDefinition.alternatingBlocks: BlockType[]` + `BlockProps.alternateDark?: boolean`, вычисляется в `page-renderer.tsx` по позиции блока среди реально включённых блоков этого набора типов) — заменило хардкод "этот тип блока всегда тёмный". Причина: выключение одного блока в админке иначе оставляло два блока одного цвета подряд. Затрагивает только шаблоны, где задан `alternatingBlocks` (сейчас — Tuscany, Old Money); остальные шаблоны это поле не используют.
- `BlockProps` пополнился `coverPhotoUrl?: string` — контентное фото обложки (`blocksConfig.cover.photoUrl`) прокидывается и в другие блоки шаблона (используется в Old Money Timer), не дублируется как отдельное поле контента.
- Ни одно архитектурное решение из CLAUDE.md в этом этапе не менялось — только визуальные/шаблонные добавления поверх существующей архитектуры.

## Design Decisions

- **Moody Paper**: рваный край — реальная фотографированная текстура (обрезки `paper.png`, предоставленного пользователем), а не CSS `clip-path` — это осознанный "анти-паттерн", которого проект теперь явно избегает для любых будущих декоративных текстур (см. CLAUDE.md §11). Шрифт — EB Garamond + Golos Text (Unbounded пробовали первым, отклонён как "слишком дженерик боевой санс"). Тёмный `primary` (`#1E2118`) — базовый фон страницы; бумажный `background` (`#F1F0EC`, сэмплирован с реального фото) виден только внутри карточек `TornCard`. Финальный `accent` — `#6B6650` (приглушённый оливково-серый, в тон бумаге); первая попытка `#A9764F` (тёплая терракота) была отклонена как выделяющаяся на фоне бумаги.
- Приём "оверсайз + `overflow-hidden` + negative-translate" (`w-[130%] max-w-none -translate-x-[11.5%]` внутри `overflow-hidden`) — общий способ прятать физические углы полноширинных декоративных фото-текстур на краях экрана; закреплён как паттерн для будущих full-bleed декоративных изображений.
- Договорённость по процессу: если задача требует найти новое фото/декоративную текстуру (рваная бумага и т.п.), всегда сначала предлагать несколько вариантов со ссылками и дожидаться согласования, не выбирать и не встраивать самостоятельно (см. CLAUDE.md §9–10; было применено при поиске текстуры Moody Paper и должно применяться дальше).
- Pink Sketch: финальная рамка фото — тонкая рамка (`border`) + блюрная тень (`shadow-[6px_6px_10px_var(--color-accent)]`), не жёсткая 0-blur тень и не декоративная wobbly SVG-рамка (обе пробовались и отклонены).
- Демо-сайты (шаг 11): один и тот же контент/фото на всех 5 шаблонах — сознательный выбор, чтобы сравнение стилей было "при прочих равных". Фото — Pexels, подобраны и согласованы с пользователем; часть первого набора отклонена и заменена по явному критерию "европейская внешность, не полные типажи, как на изначальном фото обложки". Имена демо-пары ("Александр и Мария") взяты из фолбэка публичного конфигуратора, а не придуманы отдельно. Превью-миниатюры на лендинге — реальные скриншоты каждого демо-сайта (не стоковое фото — оно одно и то же у всех 5 и не различало бы стили визуально).

## Known Issues

- **Moody Paper: формальный `/feature-review` не пройден до конца.** Уже закоммичен (`6a6fd84`) без браузерного визуального QA на 3 брейкпоинтах (desktop/tablet/mobile) и без проверки рендера кириллицы в EB Garamond/Golos Text через сам скилл — только код-ревью + статика. Пользователь пока сознательно отложил докат ("щас пока хватит, но потом главное не забыть") — всё ещё не сделано.
- Дата свадьбы в демо-сайтах захардкожена (`WEDDING_DATE` в `prisma/seed-demos.ts`, `2027-06-12`) — таймер на демо в какой-то момент "закончится". Нужно будет когда-нибудь перезапустить `npm run seed:demos` с обновлённой датой.
- Фото демо-сайтов хотлинкаются с `images.pexels.com` (внешняя зависимость) — если Pexels удалит/переименует конкретный файл, картинка на демо перестанет грузиться (не упадёт — `isRenderableUrl`/`next/image` деградируют мягко, просто пропадёт фото).
- В этой рабочей среде (Windows/эта сессия) сломан `resize_window` у браузерного инструмента — визуальное QA на tablet/mobile брейкпоинтах в рамках `/feature-review` для шага 11 пришлось пропустить, проверено только на desktop-разрешении. Раскладка/адаптивность самих шаблонов при этом не менялась (уже проверялась на шаге 10).

## Important Context

- Договорённость по процессу для крупных этапов (новый шаблон, архитектурное изменение): полный цикл `/feature-review` (код-ревью + статика + визуальное QA в браузере на 3 брейкпоинтах + проверка кириллицы) → отчёт → **стоп и явное одобрение пользователя** → только потом коммит. Для Moody Paper этот цикл был пройден не полностью (код-ревью + статика — да, визуальное QA в браузере и стоп-гейт перед коммитом — пропущены, коммит `6a6fd84` ушёл раньше времени); визуальное качество было проверено вручную пользователем раньше по ходу правок, но не через формальный `/feature-review`. Для следующих крупных этапов — использовать `/feature-review` целиком.
- Ветка `master` опережает `origin/master` (точное число коммитов растёт с каждым коммитом — смотреть `git status -sb`, не фиксировать здесь) — пуш ни разу не запрашивался и не выполнялся.
