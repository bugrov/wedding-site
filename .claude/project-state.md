# Project State

## Current Goal

Свадебный сайт-конструктор: лендинг → лид → админка (согласование/оплата) → публичный сайт на поддомене. Сейчас в фокусе — шаг 10: набор из 5 визуальных направлений (шаблонов) конструктора.

## Current Milestone

Шаг 10 "5 шаблонов" — **закрыт**. Все 5 шаблонов реализованы, отревьюены (`code-review-expert`, замечаний не найдено) и закоммичены (`6a6fd84`).

## Completed

- 5 шаблонов реализованы и зарегистрированы в `lib/templates/registry.ts`: `tuscany`, `old-money`, `editorial-bw`, `pink-sketch`, `moody-paper` — у каждого свой рефборд и "фишка" (см. CLAUDE.md §9–10).
- Illustrated Storybook (несостоявшаяся 5-я попытка) — реализован, отклонён пользователем как не подходящий эстетически, полностью удалён (`c05dfca`).
- Архитектурный баг чередования фона блоков исправлен: было хардкожено по типу блока, стало — динамически по позиции среди включённых блоков (`alternatingBlocks`/`alternateDark`, `00b8aba`). Применено к Tuscany и Old Money.
- Pink Sketch: рамка фото доведена итеративно (wobbly SVG → прямая рамка → чёткая тень → тонкая рамка + блюр-тень), поправлен клиппинг `SquigglyDivider`.
- Old Money: обложка и RSVP переработаны под рефборд, доработан таймер (переиспользует `coverPhotoUrl`).
- Moody Paper: реализован (переписан с нуля после отклонённой v1), отревьюен и закоммичен вместе с `CLAUDE.md` и этим файлом (`6a6fd84`).

## In Progress

Ничего — шаг 10 закрыт, следующий этап ещё не начат.

## Next Steps

1. Шаг 11: реальные демо-сайты с реальными фото/контентом для каждого шаблона (уже упомянуто как следующий логический шаг в комментарии `components/landing/examples.tsx`, ещё не начат) — дождаться, пока пользователь явно даст старт.
2. Ветка `master` на 43 коммита впереди `origin/master` — пуш не запрашивался; уточнить у пользователя, когда пушить.

## Architecture Decisions

- **Динамическое чередование фона** (`TemplateDefinition.alternatingBlocks: BlockType[]` + `BlockProps.alternateDark?: boolean`, вычисляется в `page-renderer.tsx` по позиции блока среди реально включённых блоков этого набора типов) — заменило хардкод "этот тип блока всегда тёмный". Причина: выключение одного блока в админке иначе оставляло два блока одного цвета подряд. Затрагивает только шаблоны, где задан `alternatingBlocks` (сейчас — Tuscany, Old Money); остальные шаблоны это поле не используют.
- `BlockProps` пополнился `coverPhotoUrl?: string` — контентное фото обложки (`blocksConfig.cover.photoUrl`) прокидывается и в другие блоки шаблона (используется в Old Money Timer), не дублируется как отдельное поле контента.
- Ни одно архитектурное решение из CLAUDE.md в этом этапе не менялось — только визуальные/шаблонные добавления поверх существующей архитектуры.

## Design Decisions

- **Moody Paper**: рваный край — реальная фотографированная текстура (обрезки `paper.png`, предоставленного пользователем), а не CSS `clip-path` — это осознанный "анти-паттерн", которого проект теперь явно избегает для любых будущих декоративных текстур (см. CLAUDE.md §11). Шрифт — EB Garamond + Golos Text (Unbounded пробовали первым, отклонён как "слишком дженерик боевой санс"). Тёмный `primary` (`#1E2118`) — базовый фон страницы; бумажный `background` (`#F1F0EC`, сэмплирован с реального фото) виден только внутри карточек `TornCard`. Финальный `accent` — `#6B6650` (приглушённый оливково-серый, в тон бумаге); первая попытка `#A9764F` (тёплая терракота) была отклонена как выделяющаяся на фоне бумаги.
- Приём "оверсайз + `overflow-hidden` + negative-translate" (`w-[130%] max-w-none -translate-x-[11.5%]` внутри `overflow-hidden`) — общий способ прятать физические углы полноширинных декоративных фото-текстур на краях экрана; закреплён как паттерн для будущих full-bleed декоративных изображений.
- Договорённость по процессу: если задача требует найти новое фото/декоративную текстуру (рваная бумага и т.п.), всегда сначала предлагать несколько вариантов со ссылками и дожидаться согласования, не выбирать и не встраивать самостоятельно (см. CLAUDE.md §9–10; было применено при поиске текстуры Moody Paper и должно применяться дальше).
- Pink Sketch: финальная рамка фото — тонкая рамка (`border`) + блюрная тень (`shadow-[6px_6px_10px_var(--color-accent)]`), не жёсткая 0-blur тень и не декоративная wobbly SVG-рамка (обе пробовались и отклонены).

## Known Issues

- Демо-сайты с реальным контентом для каждого шаблона (упомянутые в комментарии `examples.tsx` как "step 11") пока не существуют — свотчи только иллюстративные, ссылка ведёт на `#configurator`, а не на живой пример.

## Important Context

- Договорённость по процессу для крупных этапов (новый шаблон, архитектурное изменение): не коммитить, пока пользователь не одобрит финальный результат и не пройдено ревью по скиллу `code-review-expert` — соблюдено для Moody Paper (одобрение → ревью → коммит `6a6fd84`). Действует и для следующих крупных этапов.
- Ветка `master` сейчас на 43 коммита впереди `origin/master` — пуш не запрашивался и не выполнялся.
